# sbox-cli

Project-scoped Docker sandboxes for development and AI-agent workflows.

## Language

**Sbox CLI**:
The user-facing command-line tool named `sbox`.
_Avoid_: Calling the command `sbox-cli`

**Sandbox**:
A project-scoped Docker container that can be created, listed, and resumed later.
_Avoid_: Box, environment
